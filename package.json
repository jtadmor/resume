{
  "name": "yoel-tadmor-resume",
  "version": "11/18/2019",
  "scripts": {
    "build": "node build && prettier resume.html --write",
    "test": "yarn run build && open resume.html"
  },
  "devDependencies": {
    "jsdom": "^15.2.1",
    "prettier": "^1.19.1"
  }
}
